export class AddressBookModel {

    public constructor(init?: Partial<AddressBookModel >) {
        Object.assign(this, init);
    }
    // user_id:Number;
    // address_book_id:Number;
    // email_id: string;
    // first_name: string;
    // last_name: string;
    // contact_no: string;
    // is_active:boolean
}
